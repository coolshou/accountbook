<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:background="@color/background"
    android:clipChildren="true">

    <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/imageView"
            android:src="@drawable/chicken"
            android:layout_alignParentBottom="true"
            android:layout_marginBottom="27dp"/>

    <LinearLayout
            android:orientation="vertical"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_toRightOf="@+id/imageView"
            android:layout_margin="@dimen/activity_vertical_margin">

	    <Button
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:text="@string/mainoptions_create_new_epense"
                android:id="@+id/main_button_new_expense"
                android:layout_above="@+id/button2"
                android:layout_marginBottom="15dp"
                android:layout_alignLeft="@+id/button3"
                android:clickable="false"
                android:background="@color/main"
                android:textColor="@color/font_white"
                android:layout_gravity="center"
                android:drawableLeft="@android:drawable/ic_menu_camera"
                />

	    <Button
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:text="@string/mainoptions_browse_statistics"
                android:id="@+id/main_browse_statistics"
                android:layout_above="@+id/button3"
                android:layout_alignLeft="@+id/button"
                android:layout_marginBottom="15dp"
                android:background="@color/main"
                android:textColor="@color/font_white"
                android:layout_gravity="center"
                android:drawableLeft="@android:drawable/ic_menu_view"/>

	    <Button
	            android:layout_width="fill_parent"
	            android:layout_height="wrap_content"
	            android:text="@string/mainoptions_sync_with_dropbox"
	            android:id="@+id/main_button_sync"
	            android:background="@color/main"
	            android:textColor="@color/font_white"
	            android:drawableLeft="@android:drawable/ic_menu_upload"
	            android:clickable="false"/>

    </LinearLayout>

</RelativeLayout>
